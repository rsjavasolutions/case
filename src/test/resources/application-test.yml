spring:
  main:
    allow-bean-definition-overriding: true
  sleuth:
    enabled: false
    messaging:
      enable: false
    http:
      enabled: false
    jpa:
      show-sql: true
      properties:
        hibernate:
          format_sql: true

